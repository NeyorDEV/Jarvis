{
  "permissions": {
    "allow": [
      "PowerShell(Get-ChildItem \"$env:LOCALAPPDATA\" -ErrorAction SilentlyContinue | Select-Object Name)",
      "Bash(git -C /n/JARVIS ls-files)",
      "Bash(git -C /n/JARVIS status --porcelain --ignored=no)",
      "Bash(git -C /n/JARVIS diff --stat main2.py)",
      "Bash(git -C /n/JARVIS diff main2.py)",
      "Bash(git rm *)"
    ]
  }
}
